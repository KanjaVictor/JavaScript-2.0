var add = 1 + 1;

var subtract = 2 - 1;

var multiply = 2 * 2;

var divide = 10 / 5;

var increment = 10;
increment++; //This is equivalent to i + 1;

var decrement = 10;
decrement--; //This is equivalent to d - 1;

var remainder = 48 % 2;//The answer would be 0. Since we're looking at the remainder.
